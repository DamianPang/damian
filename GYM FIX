In gym.html, find this line:
const PC_SYNCED_KEYS = ['po_coach_v1', 'po_coach_workout_done', 'po_coach_weights', 'po_coach_photos'];

Remove 'po_coach_photos' from the array so it reads:
const PC_SYNCED_KEYS = ['po_coach_v1', 'po_coach_workout_done', 'po_coach_weights'];

Then commit ("Stop syncing photos: blows localStorage quota") and push to main.


THEN in supabase paste this in the SQL...


update app_state
set data = data - 'po_coach_photos'
where key = 'po-coach';
